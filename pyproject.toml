[project]
name = "tmux-harness"
version = "0.1.0"
description = "Project BareMetal-Tmux: stateful local MCP orchestration harness over stdio"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.2.0",
    "fastmcp>=2.0.0",
]
